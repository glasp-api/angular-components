import { NgDirective } from './ng.directive';

describe('NgDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDirective();
    expect(directive).toBeTruthy();
  });
});
