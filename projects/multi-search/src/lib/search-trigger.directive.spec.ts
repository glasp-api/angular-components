import { SearchTriggerDirective } from './search-trigger.directive';

describe('SearchTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
